// Aggregated exports for commonly used small UI components.
// Keeps `src/components` organized under `common` while allowing
// existing imports (from `src/components/...`) to continue working by
// re-exporting where appropriate.

export { default as FormInput } from "../FormInput";
export { default as LoadingSkeleton } from "../LoadingSkeleton";
export { default as StatCard } from "../StatCard";
export { default as StockStatus } from "../StockStatus";
export { default as Topbar } from "../Topbar";
